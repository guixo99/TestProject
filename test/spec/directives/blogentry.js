'use strict';

describe('Directive: BlogEntry', function () {

  // load the directive's module
  beforeEach(module('testProjectApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
